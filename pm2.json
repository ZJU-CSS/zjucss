{
  "apps": [
    {
      "name": "deckgl",
      "script": "set PORT=5555 Host=10.72.100.6 && react-scripts start", // 后端启动
      "cwd": "/home/zhiguangda/ITS/deckgl", // 目录
      "exec_mode": "fork", // 模式
      "max_memory_restart": "1G", // 最大内存重启
      "autorestart": true, // 自动重启
      "env": {} // 自己去配置一些环境变量
    }
  ]
}
